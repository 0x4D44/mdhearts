# 2025.11.01 – Commit Plan (Continue-On-Main)

Context
- Instead of branching, we proceed on main guarded by runtime feature flags.

Grouped Commit Outline
1) feat(hard): gate Stage1/2 behind runtime flags; add CLI toggles
   - Files: `crates/hearts-app/src/bot/play.rs`, `crates/hearts-app/src/cli.rs`.
2) tools(ci): add eval + compare wrappers; enable smokes with flags; manual CI eval
   - Files: `tools/*.sh`, `.github/workflows/ci.yml`, `.github/workflows/eval.yml`.
3) docs: feature-flag flow, eval wrappers, CI eval; refresh Stage1 README
   - Files: `README.md`, `docs/*`, Stage 1 README and plans.
4) artifacts(stage1): ultra-smokes + compare outputs with indexes and summaries
   - Files: `designs/tuning/stage1/smoke_release/*`, `designs/tuning/stage1/compare_release/*`, `designs/tuning/eval_summaries/*`.
5) chore(journal): add/update JRN entries
   - Files: `wrk_journals/*`.

Helper
- Use `tools/commit_plan_on_main.sh` (DRY-RUN by default; set `RUN=1` to execute) to stage and commit in the order above.

Notes
- Feature flags default OFF; CI smokes enable flags explicitly.
- Eval wrappers write dated summaries and indexes for reviewer convenience.
