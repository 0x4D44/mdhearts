# Code Coverage Report
**Date:** 2026-01-01
**Current Coverage:** 48.43% (Lines)

## 1. Executive Summary

The current code coverage for the `mdhearts` project is **48.43%**. This relatively low percentage is primarily driven by two large modules in `hearts-app` that have 0% coverage: `src/platform/win32.rs` (4465 lines) and `src/cli.rs` (1795 lines). 

The core game logic (`hearts-core`) is well-tested with >97% coverage. The bot logic (`hearts-app/bot`) is partially covered (~70-90%), but complex search and play logic has gaps.

To achieve the target of **98% coverage**, we must:
1.  Implement comprehensive integration tests for the CLI to cover `cli.rs`.
2.  Expand unit tests for `search.rs`, `play.rs`, and `endgame.rs` to cover edge cases.
3.  Exclude the platform-specific UI code (`win32.rs`) from the coverage metrics, as it requires a windowing environment and is not strictly "business logic". If excluded, the effective coverage would rise significantly, making the 98% target achievable for the remaining logic.

## 2. Detailed Analysis

### 2.1. hearts-core (Target: Maintain >98%)
*   **Current Status:** Excellent. Most modules are 100% or near it.
*   **Gaps:**
    *   `model/rank.rs` (73.33%): Missing some formatting or conversion edge cases.
    *   `model/suit.rs` (80.00%): Similar to rank.
    *   `game/match_state.rs` (84.06%): Some state transitions might be missed.

### 2.2. hearts-ui (Target: >95%)
*   **Current Status:** Good (48.43% - wait, table says `hearts-ui` total is misleading in summary, let's look at files).
    *   `resource.rs`: 75.00%.
    *   `theme.rs`: 97.06%.
*   **Gaps:** Minor. `resource.rs` needs a few tests for error paths.

### 2.3. hearts-app (Target: Improve significantly)
*   **CLI (`cli.rs`): 0.00%**. This is a major deficit. It handles argument parsing and command dispatch.
    *   *Remediation:* Create a `tests/cli_integration.rs` to run the binary with various flags (smoke tests) and verify exit codes/output.
*   **Platform (`platform/win32.rs`): 0.00%**.
    *   *Remediation:* This is Windows API boilerplate. Testing it requires a GUI session. **Recommendation:** Exclude from coverage report using `#[cfg(not(test))]` or ignore patterns.
*   **Bot Logic:**
    *   `bot/search.rs` (66.27%): Deep search logic. Needs tests for pruning, transposition table hits/misses, and time management.
    *   `bot/play.rs` (74.20%): Heuristic play. Needs tests for specific card combinations and fallback logic.
    *   `bot/endgame.rs` (75.00%): Endgame solver. Needs tests for specific endgame puzzles.
*   **Controller (`controller.rs`): 76.80%**. Game loop orchestration. Needs tests for timeout handling, error states, and specific state machine transitions.

## 3. Coverage Improvement Strategy

1.  **Exclude Platform Code:** Mark `hearts-app/src/platform/win32.rs` to be ignored by coverage tools. This immediately corrects the baseline to reflect the *testable* code.
2.  **CLI Integration Tests:** Write a suite of tests that invoke the application CLI in a subprocess (or via a library entry point if possible) to cover `cli.rs`.
3.  **Refine Bot Tests:** Systematically add unit tests for `search.rs` and `play.rs` branches that are currently missed (often error handling or rare optimization paths).
4.  **Core Polish:** Add the few missing tests for `hearts-core` to hit 100% there.

## 4. Adjusted Baseline (Estimate)
If `win32.rs` (~4500 lines) is removed from the denominator (~17300 lines), the total lines become ~12800.
The current covered lines are ~8900.
New Baseline ~= 8900 / 12800 = **~69.5%**.

Recovering `cli.rs` (~1800 lines) pushes us to:
(8900 + 1800) / 12800 = **~83.5%**.

The remaining 15% (~1900 lines) must come from deep diving into `bot` and `controller` logic.
